{
  "rules": {
    ".read": false,
    ".write": false,

    "presence": {
      ".read": true,

      "$connection": {
        ".write": "!data.exists() || !newData.exists()",

        ".validate": "newData.hasChildren(['at']) && newData.child('at').isNumber() && newData.children().length === 1",

        "at": {
          ".validate": "newData.isNumber()"
        },

        "$other": {
          ".validate": false
        }
      }
    }
  }
}
