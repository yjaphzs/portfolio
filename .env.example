# ─────────────────────────────────────────────────────────────────────────────
# Copy to `.env` and fill in. `.env` is gitignored; this file is the template.
#
#   cp .env.example .env
#
# Next only exposes variables prefixed with NEXT_PUBLIC_ to the client, and it
# inlines them into the built bundle at build time. Everything here is therefore
# PUBLIC — never put a secret in this file or in .env.
#
# The site is a static export, so there is no server to read these at runtime:
# they must exist in the CI environment where the bundle is built. Add them under:
#   Settings → Secrets and variables → Actions → Variables
# and reference them in .github/workflows/deploy.yml, e.g.
#   env:
#     NEXT_PUBLIC_FIREBASE_API_KEY: ${{ vars.NEXT_PUBLIC_FIREBASE_API_KEY }}
# Use "Variables", not "Secrets" — these are not secret, and Actions masks
# secrets in logs which makes build issues harder to debug.
# ─────────────────────────────────────────────────────────────────────────────

# ── Firebase — powers the "N people viewing now" presence counter ────────────
#
# Get these from the Firebase console:
#   Project settings → General → Your apps → SDK setup and configuration → Config
#
# The presence widget uses Realtime Database ONLY. Firestore is not used and is
# not a substitute: it has no server-side onDisconnect, so a closed tab would
# never clear. If you create the project fresh, add a Realtime Database (not just
# Firestore) or NEXT_PUBLIC_FIREBASE_DATABASE_URL will not exist.
#
# The Firebase web API key is an identifier, not a credential — it is designed to
# ship publicly. Access control comes entirely from Database Rules, so set them:
#
#   {
#     "rules": {
#       "presence": {
#         ".read": true,
#         "$conn": {
#           ".write": "!data.exists() || !newData.exists()",
#           ".validate": "newData.hasChildren(['at']) && newData.child('at').isNumber()"
#         }
#       }
#     }
#   }
#
# Leave every value below blank and the counter simply stays hidden — the site
# builds and runs fine without Firebase. Presence is strictly optional.

NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Must be the Realtime Database URL, NOT the Firestore one. Region-dependent:
#   https://your-project-default-rtdb.firebaseio.com              (us-central1)
#   https://your-project-default-rtdb.asia-southeast1.firebasedatabase.app
NEXT_PUBLIC_FIREBASE_DATABASE_URL=

# storageBucket and messagingSenderId appear in the console's config block but
# are not needed for Realtime Database — omitted deliberately.

# ── App Check — optional attestation for the above ────────────────────────────
#
# Proves that reads/writes to Realtime Database and Firestore come from this
# site's own client, not a scripted request replaying the public config above.
# Leave blank and everything behaves exactly as without App Check.
#
# Setup:
#   1. Firebase console → Build → App Check → register this web app using the
#      "reCAPTCHA v3" provider, and grab the site key it gives you.
#   2. Put that site key below.
#   3. Once you've confirmed real traffic is minting tokens (App Check →
#      Apps → metrics), flip Realtime Database and Firestore to "Enforced" on
#      the App Check → APIs tab. Enforcing before that point locks everyone out.
#
# For local dev, App Check → Apps → this app → "Manage debug tokens", generate
# one, put it below as NEXT_PUBLIC_FIREBASE_APP_CHECK_DEBUG_TOKEN so it's stable across
# runs. Leave that blank and a fresh token is minted and logged to the console
# each run instead — register whatever it prints, or dev traffic stays
# unattested until you do.
NEXT_PUBLIC_FIREBASE_APP_CHECK_SITE_KEY=
NEXT_PUBLIC_FIREBASE_APP_CHECK_DEBUG_TOKEN=

